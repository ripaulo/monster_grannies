<div><h2>Choose your granny</h2></div>

<ul>
  <%@grannies.each do |granny| %>
    <%= link_to granny_path(granny) do %>
      <div class='card'>
        <%= cl_image_tag granny.photo.key %>
        <li><%= granny.first_name %></li>
        <li><%= granny.last_name %></li>
        <li><%= granny.description %></li>
        <li><%= granny.price%>€</li>
        <li><%= granny.location%></li>
        <li><%= (Date.today - granny.birth_date).to_i / 365 %> years young</li>
      </div>
    <%end %>
  <%end %>
</ul>
